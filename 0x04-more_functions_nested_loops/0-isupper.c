#include "main.h"

/**
 * _isupper - checks the uppercase characters
 * @c: the character to check
 *
 * Return: 1 if c is upperclass, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
